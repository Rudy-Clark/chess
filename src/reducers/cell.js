const initialState = [];

const cell = (state = initialState, action) => {
  switch (action.type) {
    case 'SELECT':
      return state;
    default:
      return state;
  }
};

export default cell;
